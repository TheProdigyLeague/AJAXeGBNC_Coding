function createProblem(message, node, severity = 'error') {return{message, node, severity,};} module.exports = {meta: {type: 'problem', docs: {description: 'custom-eslint-rule',},}, create(context) {return {@param {string} SomeNode(node){const problem = createProblem('Devs Created Problem', node); context.report(problem);},};},};
